{
  "build": {
    "buildCommand": "cd backend && composer install --no-dev --optimize-autoloader && rm -rf node_modules package-lock.json && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
